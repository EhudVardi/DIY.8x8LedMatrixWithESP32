#include "BaseAnime.h"

BaseAnime::BaseAnime(int size) {
    // : N(size) {
    // int numBytes = (N * N) / 8;
    // matrix = new uint8_t[numBytes]();
}

int BaseAnime::getStepDuration() {
    return stepDuration;
}
